# Production Database Configuration
# This file is a template for production environment variables
# Set these variables in your hosting platform (Vercel, Railway, etc.)

# PostgreSQL (Vercel Production)
# Set this in Vercel Environment Variables
DATABASE_URL="postgresql://user:password@production-host:5432/dbname?schema=public&connection_limit=5"

# SQL Server (Production)
# DATABASE_URL="sqlserver://production-host:1433;database=dbname;user=username;password=password;encrypt=true;trustServerCertificate=false"

# Production Notes:
# - Always use connection pooling in production
# - For Vercel: DATABASE_URL is automatically set when you connect Postgres storage
# - For SQL Server: Use Azure SQL or self-hosted with proper SSL certificates
# - Enable SSL/TLS encryption in production
